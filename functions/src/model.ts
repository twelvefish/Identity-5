export type User = {
    id: string
    name: string
    picUrl: string
    lineId: string
}

export type Identity = {
    id: string
    userId: string
    lineId: string
    lineName: string
    gameId: string
    gameName: string
    level: string
    remarks: string
    timestamp: number
}